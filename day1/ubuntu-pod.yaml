apiVersion: v1
kind: Pod
metadata:
  labels:
    os: ubuntu22
  name: ubuntu-pod
spec:
  containers:
  - image: ubuntu
    name: ubuntu-pod
    command: ['sh','-c','sleep 3d']
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
