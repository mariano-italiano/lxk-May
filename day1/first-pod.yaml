apiVersion: v1
kind: Pod
metadata:
  name: first-pod 
  labels:
    kubernetes.io/test: testpod
    app: web
  annotations:
    test-anno: test value  
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80

